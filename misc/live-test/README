This regression test suite verifies that a live Pogo server is running 
correctly.

It needs to be pointed to a Pogo server's web interface and called via
'make' like

    export POGO_URL=http://some.pogoserver.somewhere
    make

to run successfully.
